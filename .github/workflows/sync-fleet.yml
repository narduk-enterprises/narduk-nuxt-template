name: Sync Fleet (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated entrypoint
    runs-on: ubuntu-latest
    steps:
      - name: Fail with migration guidance
        run: |
          echo "::error::sync-fleet.yml is deprecated. Dispatch template-sync-bot.yml instead."
          exit 1
